from django.apps import AppConfig

class StudentAdvancedConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'student_advanced'  # Make sure this matches the app folder name